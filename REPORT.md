# تقرير التعديلات — Dzair Volley

> آخر تحديث: 24 سبتمبر 2026. هذا الملف يشرح ما تغيّر، ولماذا، وكيف تُشغّله، **وما الذي لم أستطع اختباره**.

## تحديث لاحق — 24 سبتمبر 2026: إلغاء وسطاء CORS نهائياً، اعتماد حصري على مفتاح Highlightly المباشر

- **المشكلة**: النتائج والمباريات العالمية كانت تظهر فارغة أو تبقى عالقة على "جاري
  التحميل" لأن المصدر الافتراضي (تغذية SofaScore العامة) كان يمر إلزامياً عبر وسطاء
  CORS خارجيين (`corsproxy.io` ثم `api.allorigins.win`) — وهما خدمتان مجانيتان غير
  مملوكتين للمشروع، عرضة للبطء/الانقطاع/الحظر في أي وقت دون سابق إنذار.
- **الحل**: أُزيل مصدر SofaScore ودالة `fetchJsonWithFailover()` وقائمة `CORS_RELAYS`
  بالكامل من `src/services/volleyballApi.js`. أصبح **Highlightly Volleyball API**
  (`volleyball.highlightly.net`) — عبر `VITE_HIGHLIGHTLY_API_KEY` أو بديله
  `VITE_RAPIDAPI_KEY` — هو **المصدر الوحيد** للمباريات الحيّة/المجدولة، ويُستدعى
  **مباشرة من متصفح الزائر** بالمفتاح الحقيقي دون أي وسيط. مفاتيح Supabase
  (`VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY`) المستخدمة للتخزين المؤقت المشترك
  (`volleyball_cache`) بقيت كما هي — وهي أصلاً مفاتيح مباشرة بلا وسيط.
- تحديث نطاقات `connect-src` في CSP (`vite.config.js`, `public/_headers`,
  `vercel.json`, `deploy/nginx.conf`, `deploy/apache.htaccess`) بحذف
  `api.sofascore.com` و`corsproxy.io` و`api.allorigins.win` منها، بما أن الموقع لم
  يعد يتصل بها إطلاقاً.
- لا تغيير في أي منطق أو ميزة أخرى في المشروع: التخزين المؤقت المحلي
  (`localStorage`, 4 ساعات)، التخزين المؤقت المشترك عبر Supabase، قاطع الدائرة عند
  تجاوز الحصة (HTTP 429/403)، ونتائج `volleyballSeed.js` الاحتياطية — كل ذلك بقي كما
  هو دون حذف أو تعديل.
- ✅ تم التحقق من نجاح البناء (`npm run build`) بدون أي أخطاء بعد هذا التعديل.

## تحديث لاحق — 23 سبتمبر 2026: مصادر المباريات + تنظيف الشعارات

> ملاحظة: الوصف التالي تاريخي — راجع التحديث أعلاه (24 سبتمبر 2026) للوضع الحالي، إذ
> أُزيل مصدر SofaScore ووسطاء CORS المذكورين هنا بالكامل.

- **مصادر المباريات العالمية**: `src/services/volleyballApi.js` لم يعد يعتمد حصرياً على
  مفتاح Highlightly/RapidAPI المؤقت (القسم "مفتاحا Highlightly وRapidAPI" أدناه ما زال
  صحيحاً تاريخياً لكنه لم يعد الوصف الكامل). المصدر الافتراضي الآن هو تغذية Volleyball
  العامة من SofaScore (`api.sofascore.com`)، بلا مفتاح، عبر سلسلة عبور CORS
  (`corsproxy.io` ثم `api.allorigins.win`) لأن ذلك المضيف لا يرسل ترويسات CORS. مفتاح
  Highlightly/RapidAPI أصبح **مصدراً إضافياً اختيارياً** فقط يُدمج مع نتائج SofaScore
  ويُنقّى من التكرار — الموقع لم يعد يتوقف أو يُظهر حالة "غير مُهيّأ" إن غاب. تفاصيل أكثر
  في README.md ← "Live volleyball data".
- **الشعارات**: مجلد `public/` أصبح يحوي حصراً `logo1.png` (أيقونة التبويب) و`logo2.jpeg`
  (الشعار الرئيسي)؛ حُذفت `public/logo.jpeg` و`public/logo1.jpeg` و
  `public/dzair-volley-icon.svg` (غير مستخدمة). صورة الاحتياط (placeholder) التي كان
  `volley.sh` ينسخها عند غياب شعار المستخدم انتقلت خارج `public/` إلى
  `branding-assets/placeholder-logo1.png` و`branding-assets/placeholder-logo2.jpeg` حتى
  لا تُنشر على الموقع. ملفات الأمان/الفهرسة (`robots.txt`, `_headers`, `ai.txt`,
  `.well-known/security.txt`) أُبقيت كما هي عمداً — ليست شعارات، وحذفها كان سيُعطّل ترويسات
  الأمان الموصوفة في هذا التقرير.

## 0) خطوات التشغيل (مرة واحدة)

1. فك الضغط: `./volley.sh` ثم `cd dzair-volley && npm install && npm run build`.
2. في Supabase ← **Authentication → Users**: تأكد أن حساب الأدمن موجود.
3. في Supabase ← **SQL Editor**: الصق **كامل** ملف `supabase/schema.sql` ← Run.
   يسجّل الملف `abdelhakbetch@gmail.com` كأدمن (القسم 1b). غيّر البريد إن كان حسابك مختلفاً، وأعد تشغيل ذلك القسم إن أنشأت الحساب بعد التشغيل الأول.
4. في Supabase ← Authentication ← Sign In: **عطّل** "Allow new users to sign up". غيّر كلمة مرور الأدمن إلى كلمة قوية جديدة.
5. انشر الموقع مع ملف الترويسات المناسب لاستضافتك (`public/_headers` لـ Netlify/Cloudflare Pages، `vercel.json` لـ Vercel، أو `deploy/nginx.conf` / `deploy/apache.htaccess`)، وطبّق `deploy/CLOUDFLARE_WAF.md`.

> ⚠️ إذا استخدمتَ الكود الجديد **قبل** تشغيل `schema.sql`، فسيفشل رفع الصور والفيديوهات إلى التخزين (لا توجد سياسات bucket بعد) وستظهر لك رسالة واضحة بذلك. العدّادات ستتراجع تلقائياً للسلوك القديم.

## 1) الفيديوهات (الشاشة السوداء)

**السبب الجذري:** لوحة التحكم كانت تحوّل الفيديو إلى نص base64 وتحفظه داخل عمود `video_url`. الفيديو 20MB يصبح نصاً ~27MB: يتجاوز حدود رسائل Realtime، ويثقل كل زائر (`select *`)، وكثير من المتصفحات تفشل في تشغيل رابط `data:` بهذا الحجم. كما لم يكن ممكناً لصق رابط يوتيوب/فيسبوك أبداً.

**الإصلاح:**
- `src/components/VideoPlayer.jsx` (جديد): ملفات مباشرة ← `<video controls playsInline preload="metadata">` مع تلميح `#t=0.001` ليرسم iOS Safari أول إطار بدل المربع الأسود؛ روابط المنصات (YouTube، Vimeo، Facebook، Instagram، TikTok، Dailymotion، Google Drive) ← `iframe` بـ `sandbox` يُحمَّل عند الضغط على تشغيل؛ الصفوف القديمة base64 ← تُفكّ إلى Blob وتعمل؛ أي فشل ← رسالة "تعذّر تشغيل الفيديو" + زر "فتح الفيديو".
- `src/services/mediaStorage.js` (جديد): رفع الملفات إلى Supabase Storage (bucket `site-assets`) وحفظ **الرابط فقط** في الجدول. صور JPEG تُصغَّر (حتى 1600px) وتُنزع منها بيانات EXIF.
- لوحة التحكم: رفع إلى Storage + **حقل لصق رابط فيديو** + مؤشر "جارٍ الرفع" + تنبيه صريح إذا فشل الحفظ على الخادم (سابقاً كانت تعرض "تم النشر" حتى لو رفضه الخادم).
- الروابط المسموحة للتضمين محصورة في قائمة منصات معروفة؛ لا يمكن تضمين رابط عشوائي.
- الفيديوهات تُعرض كاملة الإطار (`object-fit: contain`) وعناصر التحكم الأصلية غير محجوبة بطبقة العنوان.

## 2) صور غلاف الأخبار

**السبب:** ارتفاع ثابت 190px مع `object-fit: cover` ← قصّ لأي صورة غير أفقية.
**الإصلاح:** إطار مرن 16:9؛ الصورة كاملة (`contain`) فوق نسخة ضبابية منها تملأ الفراغ. اختُبرت بأغلفة أفقية وعمودية ومربعة وبانورامية: ظهرت الزوايا الأربع في كل الحالات.

## 3) الأمان — ما أُضيف وما هي حدوده

| الطبقة | التفاصيل |
|---|---|
| **صلاحيات الأدمن** | كانت سياسات RLS تعتبر *أي مستخدم مسجَّل* أدمن. الآن كل كتابة تتطلب `is_admin()` (جدول `admin_users`). واجهة الدخول تتحقق منها أيضاً وتُخرج الحساب غير الأدمن. |
| **كتابة الزوار** | لا يملك الزائر أي صلاحية UPDATE على الجداول. العدّادات (إعجاب/مشاهدة/تعليق) والزيارات والتقييم تمر عبر دوال RPC ضيقة: قائمة بيضاء للجداول والحقول، قيم محدودة (±1)، وحدّ معدّل لكل IP (مجزّأ MD5). دالة التقييم صارت تتحقق من مدخلاتها وتحصر المتوسط بين 1 و5. |
| **حقن SQL** | لا يوجد SQL خام في الواجهة؛ كل الاستعلامات عبر PostgREST بمعاملات، والدوال تستعمل `format(%I)` مع قوائم بيضاء. |
| **XSS** | React يهرّب كل النصوص ولا يوجد `dangerouslySetInnerHTML`. أُضيفت تنقية إضافية للمدخلات (محارف التحكم، محارف الاتجاه الخادعة، الأصفار العريضة)، وحد 500 حرف للتعليق، وقبول روابط http(s) فقط. |
| **CSP وترويسات** | CSP صارمة (`script-src 'self'`)، `X-Frame-Options`، `nosniff`، HSTS، `Permissions-Policy`، `Cross-Origin-Resource-Policy` (يمنع الـ hotlinking)، `X-Robots-Tag: noai`. تُحقن في الـ build من ملف `.env`. |
| **البوتات وسكرابرز الذكاء الاصطناعي** | `robots.txt` يحجب ~54 بوتاً (GPTBot، ClaudeBot، CCBot، PerplexityBot، Bytespider…) دون حجب Google/Bing/روابط المعاينة؛ `ai.txt`؛ قواعد nginx/Apache/Cloudflare حسب User-Agent؛ كشف المتصفحات الآلية (Selenium/Puppeteer/Playwright/headless) في الواجهة (تُعطَّل بـ `VITE_BLOCK_AUTOMATION=false`). |
| **الاستنساخ** | كسر الإطار/حجبه عند التضمين في iframe غريب؛ قفل نطاقات اختياري `VITE_ALLOWED_HOSTS`؛ منع النقر اليمين/السحب على الصور والفيديو؛ `sourcemap` مُعطَّل. |
| **بيانات الدخول** | كلمة المرور الافتراضية `123456789` وبريدك **أُزيلت من نسخة الإنتاج** (تُستبدل وقت البناء)، ولم تعد تُخزَّن في localStorage عند استخدام Supabase. |

**حدود صريحة (لا أعدك بأكثر منها):**
- المحتوى العام (أخبار/فيديوهات/نتائج) قابل للقراءة من أي أحد بالمفتاح العام؛ لا يمكن جعله سرياً. ما نفعله: منع الكتابة والتعديل، وإبطاء النسخ الجماعي وجعله قابلاً للحجب (WAF + Max rows).
- robots.txt/ai.txt يلتزم بهما الشريف فقط؛ كشف المتصفحات الآلية يوقف السكرابرز البسيطة فقط (أدوات stealth تتجاوزه). القوة الحقيقية في طبقة Cloudflare/nginx.
- منع النقر اليمين وما شابه رادع للنسخ العَرَضي وليس حماية.
- حدود المعدّل لكل IP سخيّة عمداً (شبكات الجوال في الجزائر تجمع مستخدمين كثراً خلف IP واحد).
- **مفتاحا Highlightly وRapidAPI**: أي متغير `VITE_` يُدمج في ملف JavaScript العام، فهما مكشوفان لكل زائر ويستدعي متصفح كل زائر الـ API مباشرة بنفس المفتاح المشترك. الأسلم يبقى نقل الاستدعاء كلياً إلى Supabase Edge Function (لم أنفّذها). بدلاً من ذلك، ولتخفيف استنزاف الحصة عملياً دون الحاجة لبنية خادم جديدة، أضفتُ جدول تخزين مؤقت مشترك (`volleyball_cache` في `schema.sql`، عبر `get_volleyball_cache`/`set_volleyball_cache`): كل متصفح يتحقق من آخر نتيجة ناجحة في Supabase (ومن علامة "الحصة استُنفدت" المشتركة) قبل مناداة Highlightly/RapidAPI مباشرة، فلا يستهلك الحصة فعلياً سوى أول متصفح يحتاج تاريخاً معيناً، ويُخدَم كل الزوّار الآخرين من Supabase. هذا يقلّص عدد الطلبات الفعلية للـ API الخارجي بشكل كبير دون حذف أي سلوك سابق (التخزين المحلي والتهدئة الخاصة بكل متصفح ما زالا يعملان كما هما). إن أردت الحل الأكمل (إخفاء المفتاح تماماً عن المتصفح)، فذلك يتطلب Edge Function كما ورد أعلاه. فعّل أيضاً حدّ الاستهلاك/قيود النطاق في لوحة مزوّد المفتاح وجدّده عند الاشتباه بإساءة.

## 4) الربط بـ Supabase (والمفاتيح)

المفاتيح في `.env` مطابقة حرفياً لما أرسلتَه (لم تُمَس):
`VITE_SUPABASE_URL`، `VITE_SUPABASE_ANON_KEY`، `VITE_HIGHLIGHTLY_API_KEY`، `VITE_RAPIDAPI_KEY`.

| ميزة في الموقع | الجدول / المصدر | القراءة | الكتابة | Realtime |
|---|---|---|---|---|
| الأخبار | `news` | الجميع | أدمن فقط | ✅ |
| الفيديوهات/الريلز | `reels` | الجميع | أدمن فقط | ✅ |
| العدّادات (إعجاب/مشاهدة/تعليق) | أعمدة في `news`/`reels` عبر `bump_counter()` | — | RPC محدود لكل IP | ✅ |
| المباريات والنتائج | `algerian_matches` | الجميع | أدمن فقط | ✅ |
| **الترتيب (Standings)** | يُحسب في الواجهة من `algerian_matches` | — | — | ✅ (تبعاً للمباريات) |
| شعارات البطولات | `division_crests` | الجميع | أدمن فقط | ✅ |
| اسم الموقع/الشعار/الغلاف | `site_settings` (صف واحد) | الجميع | أدمن فقط | ✅ |
| عدّاد الزوار والتقييم | `site_stats` عبر `increment_visitor_count()` و`submit_site_rating()` | الجميع | RPC فقط | ✅ |
| الصور والفيديوهات | Storage bucket `site-assets` (50MB للملف، أنواع محددة) | بالرابط العام | أدمن فقط | — |
| صلاحيات الأدمن | `admin_users` + `is_admin()` | مخفي | SQL Editor فقط | — |
| حدود المعدّل | `rate_limits` | مخفي | داخلي | — |
| تخزين مؤقت مشترك لمباريات الفوليبول (Highlightly/RapidAPI) | `volleyball_cache` عبر `get_volleyball_cache()`/`set_volleyball_cache()` | RPC فقط | RPC فقط (بحدّ معدّل) | — |

**ما ليس في Supabase (بحسب تصميم الموقع الأصلي):**
- **النتائج المباشرة وجداول المواعيد**: تأتي من Highlightly API مباشرة، لا تُخزَّن عندك.
- **نص التعليقات**: يبقى في متصفح كاتبه (localStorage)؛ العدّاد العام فقط يُزامَن. لم أنشئ جدول تعليقات لأنه يتطلب هوية/إشراف ويستحق قراراً منك.

## 5) التوافق مع الشاشات

اختُبر الموقع العام في متصفح حقيقي عند 320 و360 و390 و430 و600 و768 و820 و1024 و1280 و1440 و1920 بكسل: **لا تمرير أفقي ولا عناصر تتجاوز الشاشة**. الدرج الجانبي للوحة التحكم على الجوال يُخفى خارج الشاشة عمداً. أُضيفت قواعد وقائية (تقليص عناصر الشبكة، كسر النصوص الطويلة، عناوين مرنة، أهداف لمس مريحة، حشو أصغر تحت 360px، دعم `prefers-reduced-motion`).

## 6) ما اختبرتُه فعلاً وما لم أستطع

**اختُبر (متصفح Chrome حقيقي عبر Playwright + Supabase وهمي في الذاكرة):**
تشغيل ملف عمودي وأفقي وصف قديم base64 ✔، ملف معطوب ← رسالة بدل الشاشة السوداء ✔، رابط يوتيوب ← واجهة تشغيل ثم iframe على النطاق المسموح ✔، أغلفة الأخبار الأربعة كاملة ✔، رفع فيديو ← Storage (مسار ونوع صحيحان) دون base64 ✔، لصق رابط يوتيوب مقبول و`javascript:` مرفوض ✔، رفض حساب غير أدمن وتسجيل خروجه ✔، تنبيه فشل الحفظ على الخادم ✔، العدّادات عبر RPC دون UPDATE مباشر + تراجع للسلوك القديم ✔، حدّ 6 نقرات/دقيقة ✔، حجب المتصفحات الآلية وقفل النطاق والحجب داخل iframe غريب ✔، والتطبيق كاملاً تحت CSP الحقيقية **بلا أي انتهاك** ✔.

**لم أستطع اختباره — تحقّق منه عندك:**
1. **`npm run build` الفعلي**: بيئتي لا تصل إلى سجل npm فلم أستطع تثبيت Vite. تحققتُ بتجميع المصدر كاملاً عبر esbuild (0 أخطاء) واختبرتُ منطق إضافة CSP في `vite.config.js` بحزم بديلة. شغّل `npm run build` وأخبرني بأي خطأ.
2. **ملف `schema.sql`**: لا يوجد PostgreSQL عندي، فلم يُنفَّذ. راجعتُه سطراً بسطر وفحصتُ توازن الاقتباسات والأقواس فقط. يُنفَّذ كمعاملة واحدة، فإن فشل شيء لن يُطبَّق شيء؛ أرسل لي نص الخطأ إن ظهر.
3. **Supabase الحقيقي**: الاختبارات على نسخة وهمية من `supabase-js`؛ لم أجرّب Realtime أو Storage أو RLS على مشروعك.
4. **ترميز H.264 (mp4)** غير متاح في متصفحاتي، فاختبرتُ التشغيل بـ webm. أفضل صيغة لجمهورك: **MP4 (H.264 + AAC)**. ملفات `.mov` من iPhone بترميز HEVC قد لا تعمل في Chrome/Firefox — حوّلها إلى MP4.
5. **أجهزة iOS/Android حقيقية**، وقواعد nginx/Apache/Cloudflare (كُتبت ولم تُشغَّل).

## 7) عن شرط "عدم حذف أي سطر"

كل الإضافات جديدة (ملفات كاملة + قسم CSS مُلحَق بنهاية `index.css` بلا حذف أي سطر منه). في ملفات موجودة **استُبدل 37 سطراً بالضبط** لأن الإصلاح يتطلب ذلك، وكل منها له بديل يؤدي الوظيفة نفسها وأكثر:
- كتلة `<video>` في `ReelsSection` ← `VideoPlayer`. أُزيلت سمة `muted` (التشغيل صار بنقرة المستخدم فيُسمع الصوت) ومعالج `onClick` على الفيديو (كان سيتعارض مع أزرار التحكم الأصلية).
- ثوابت بيانات الدخول الافتراضية ← نفس القيم في التطوير و**فارغة في الإنتاج**.
- استدعاءات `onAdd*/onDelete*/onUpdate*` ← نفسها، مع معالجة نتيجة الفشل.
- `await supabase...delete()` ← نفس الحذف مع فحص الخطأ واستعادة الحالة.
- أول سطر في دوال تنقية النص ← يستدعي التنقية الأقوى ثم يكمل السلسلة القديمة كما هي.
- `plugins: [react()]` ← `[react(), securityMetaPlugin()]`.

## 8) توصيات لاحقة

- انقل مفتاحي Highlightly/RapidAPI خلف Edge Function.
- أعد رفع الفيديوهات القديمة (المخزّنة base64) عبر اللوحة لتصبح روابط Storage خفيفة؛ تعمل الآن لكنها ثقيلة.
- إن رفعتَ خطتك في Supabase فارفع حد `file_size_limit` في `schema.sql` وقيمة `MAX_VIDEO_BYTES` في `mediaStorage.js`.
- فعّل `VITE_ALLOWED_HOSTS` بعد تثبيت نطاقك النهائي.

## 9) مشغّل الفيديو Plyr

استُبدل عنصر `<video controls>` العادي في **بطاقات الريلز والنافذة المنبثقة** بمكتبة Plyr (حزمة npm باسم `plyr`، تُثبَّت مع `npm install` وتُجمَّع مع Vite؛ لا CDN بسبب سياسة CSP الصارمة، وأيقونات Plyr تُحقن داخل الصفحة).

- **جديد:** `src/components/PlyrVideo.jsx` (المشغّل)، `src/services/plyrI18n.js` (تسميات عربي/إنجليزي/فرنسي).
- **معدَّل ضمن نطاق المشغّل فقط:** `VideoPlayer.jsx` و`index.css` (قسم جديد رقمه 6 قبل أداة `.rounded-full`) و`package.json` (إضافة `plyr`).
- **الغلاف (Poster):** طبقة Plyr الخاصة بالغلاف + بقاء الغلاف ظاهرًا في النافذة حتى يُعرض أول إطار فعليًا (لا شاشة سوداء).
- **التشغيل:** الضغط على البطاقة يفتح النافذة داخل نفس اللمسة ويستدعي `play()` فورًا (يعمل مع صوت على iOS).
- **ملء الشاشة:** زر Plyr نفسه (واجهة ملء الشاشة الحقيقية، وبديل بملء النافذة على iPhone). الخروج منه يعيد المشغّل إلى النافذة، وإغلاق النافذة يعيد البطاقة بغلافها فورًا — بلا إعادة تحميل للصفحة.
- **بقي كما هو:** تثبيت `body` بـ `position: fixed`، حماية السحب للتحديث (`overscroll-behavior` / `touch-action`)، مسار الـ iframe للمنصات، مسار base64 القديم.
- **إصلاح ضمن قفل التمرير:** استعادة موضع التمرير عند الإغلاق أصبحت فورية؛ كان `behavior: 'auto'` يخضع لـ `scroll-behavior: smooth` في `html` فيظهر انزلاق من أعلى الصفحة.
- لم يُعدَّل: فيديو خلفية `Hero` ومعاينة الرفع في لوحة الإدارة (ليسا مشغّلين).
